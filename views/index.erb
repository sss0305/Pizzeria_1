<!DOCTYPE html>
<html>
<head>
	<!-- <meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title></title> -->
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">

</head>
<body>
	<h2>Welcome to Pizzeria</h2>
	<button class="btn btn-lg btn-primary" onclick="something()" >Order Now</button>
	<br><br>

	<h2>Our pizzas:</h2>
	<br>
	<br>
	<br>

	<div class="container-md">	
		<div class="row">
			<% @products.each do |product| %>
		 		
			 	<div class="col-4">
			 		<div class="row">
			 			<img src="<%= product.path_to_image %>" class="img-thumbnail" height="300">
					</div>
					<div class="row">
						<%= product.id %>. <%= product.title %>
					</div>
					<div class="row">
						<%= product.price %> USD
					</div>
					<div class="row">
						<%= product.size %> CM
					</div>
				</div>
			<% end %>

		</div>
	</div>










